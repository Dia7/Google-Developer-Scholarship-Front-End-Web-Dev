<h3>Directions:</h3>
Rewrite the following while loop as a for loop:

<code>var x = 9;</code>

<code>while (x >= 1) {</code>

 <code> console.log("hello " + x);</code>  
 
  <code>x = x - 1;</code>
  
<code>}</code>
